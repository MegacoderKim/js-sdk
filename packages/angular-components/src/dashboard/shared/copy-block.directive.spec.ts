import { CopyBlockDirective } from './copy-block.directive';

describe('CopyBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyBlockDirective();
    expect(directive).toBeTruthy();
  });
});
